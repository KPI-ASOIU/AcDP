<nav class="ui secondary pointing menu" role="navigation">
  <%= yield :navbar %>
  <div class="ui dropdown item right">
    <% if user_signed_in? %>
      <%= current_user.login %> <i class="dropdown icon"></i>
      <div class="menu">
        <%= link_to t('users.dashboard'), dashboard_path, class: 'item' %>
        <%= link_to t('util.logout'), destroy_user_session_path, :method => :delete, class: 'item' %>
      </div>
    <% end %>
  </div>
  <div class="">

  <div>
</nav>
