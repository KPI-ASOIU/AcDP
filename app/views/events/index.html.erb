<% if not @events.empty? %>
  <div class="panel panel-primary">
    <div class="panel-heading">
      <a data-toggle="collapse" data-parent="#accordion" href="#collapseOne" class="text-white">
        <%= t('util.extended_search') %>
      </a>
      <%= button_tag id: "searchBtn", class: "pull-right text-white invisible no-styling-btn no-paddings", title: I18n.t('util.search') do %>
        <span class='glyphicon glyphicon-search'></span>
      <% end %>
    </div>
    <div id="collapseOne" class="panel-collapse collapse">
      <div class="panel-body">
        <%= render 'extended_search' %>        
      </div>
    </div>
  </div>
<% end %>


<div class="panel panel-primary">
  <div class="panel-heading">
    <%= t('activerecord.models.events') %>
    <%= link_to new_event_path, class: "pull-right text-white", title: I18n.t('util.add_new_event') do %>
      <span class='glyphicon glyphicon-plus'></span>
    <% end %>
  </div>
  <% if not @events.empty? %>
    <table class="tasks table table-hover table-verticle-centered">
      <%= render partial: 'search_header', locals: { events: @events } %>
      <tbody id="tasks">
        <%= render partial: 'search_results', locals: { events: @events } %>
      </tbody>
    </table>
  <% else %>
    <p class="text-center text-muted no-conversations"><%= t('events.no_events') %></p>
  <% end %>
</div>


